language: go

matrix:
  include:
    - os: linux
      go: "1.11.x"
    - os: linux
      go: "1.10.x"
    - os: linux
      go: "1.9.x"
    - os: osx
      go: "1.11.x"
    - os: windows
      go: "1.11.x"

before_install:
  - go get -v -u github.com/dvyukov/go-fuzz/...
  
script:
  - go test ./... -short
  - go test ./...
